/** @format */

import { authController } from "@/controllers";
import { Router } from "express";

export const gameRouter = Router();

// gameRouter.post("/signup", gameController.signUp);
// gameRouter.post("/signin", gameController.signIn);
